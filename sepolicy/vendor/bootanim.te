# bootanim.te

allow bootanim video_device:chr_file rw_file_perms;
allow bootanim traced_producer_socket:sock_file write;
allow bootanim traced:unix_stream_socket connectto;

get_prop(bootanim, userspace_reboot_exported_prop)

dontaudit bootanim system_data_file:dir search;
